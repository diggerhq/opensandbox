---
title: "Introduction"
description: "Cloud sandboxes for AI agents"
---

{/* STUB — Progressive JPEG pass 1. Each section below describes planned content. */}

## What is OpenComputer

{/* One-sentence tagline: cloud sandboxes for AI agents. 3-4 sentences explaining: what a sandbox is, how it differs from containers/serverless (persistent full Linux VM, hibernates, checkpoints). */}

## Key Features

{/* Feature cards (4):
  - Claude Agent SDK built in
  - Long-running (hours/days, not minutes)
  - Checkpoint/fork like git branches
  - Full Linux VM (not a container)
*/}

<CardGroup cols={2}>
  <Card title="Claude Agent SDK built in" icon="robot">
    {/* Agent runs inside VM with full filesystem and shell access */}
  </Card>
  <Card title="Long-running" icon="clock">
    {/* Hours/days, not minutes. Install packages, build, test, iterate. */}
  </Card>
  <Card title="Checkpoint & fork" icon="code-branch">
    {/* Named snapshots. Fork new sandboxes from any checkpoint. Like git branches for VMs. */}
  </Card>
  <Card title="Full Linux VM" icon="server">
    {/* Not a container. Real VM with its own kernel, memory, disk. Hardware isolation via Firecracker. */}
  </Card>
</CardGroup>

## Install

{/* CodeGroup: npm / pip / CLI install commands. Set OPENCOMPUTER_API_KEY env var. */}

<CodeGroup>

```bash npm
npm install @opencomputer/sdk
```

```bash pip
pip install opencomputer-sdk
```

```bash CLI
brew install opencomputer/tap/oc
```

</CodeGroup>

```bash
export OPENCOMPUTER_API_KEY=your-api-key
```

## Quick Example

{/* Minimal: create sandbox -> run agent -> get result. TS + Python CodeGroup. Keep very short — quickstart has the full walkthrough. */}

## Next Steps

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    {/* Working agent in 2 minutes */}
  </Card>
  <Card title="How It Works" icon="microchip" href="/how-it-works">
    {/* Architecture: Firecracker VMs, hibernation, checkpoints */}
  </Card>
  <Card title="Agents" icon="robot" href="/agents/overview">
    {/* Run Claude inside sandboxes */}
  </Card>
  <Card title="Sandboxes" icon="box" href="/sandboxes/overview">
    {/* The compute primitive */}
  </Card>
</CardGroup>
