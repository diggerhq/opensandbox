---
title: "Quickstart"
description: "Working agent in 2 minutes"
---

{/* STUB — Progressive JPEG pass 1. Goal: one focused example, zero detours. */}

## Prerequisites

{/* API key + SDK installed (link to intro for install). */}

```bash
export OPENCOMPUTER_API_KEY=your-api-key
```

## Run an Agent

{/* Single example: create sandbox, agent.start() with a real task, stream events, print result.
    TS + Python CodeGroup. This is the ONLY example on this page.
    Python example must use sandbox.agent (NOT sandbox.commands). */}

<CodeGroup>

```typescript TypeScript
// TODO: create sandbox -> agent.start() -> stream events -> print result -> kill
```

```python Python
# TODO: create sandbox -> agent.start() -> collect_events() -> print result -> kill
```

</CodeGroup>

## What Just Happened

{/* 2-3 sentences: you created a cloud VM (Firecracker microVM), Claude ran inside it with bash and file tools, worked autonomously, and streamed events back to your code. */}

## Next Steps

<CardGroup cols={2}>
  <Card title="How It Works" icon="microchip" href="/how-it-works">
    {/* Architecture and technical decisions */}
  </Card>
  <Card title="Agents" icon="robot" href="/agents/overview">
    {/* Events, tools, multi-turn conversations */}
  </Card>
  <Card title="Sandboxes" icon="box" href="/sandboxes/overview">
    {/* Lifecycle, running commands, files */}
  </Card>
  <Card title="Checkpoints" icon="code-branch" href="/sandboxes/checkpoints">
    {/* Snapshot and fork VMs */}
  </Card>
</CardGroup>
