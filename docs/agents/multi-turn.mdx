---
title: "Multi-turn"
description: "Follow-ups, resume, and session management"
---

{/* STUB — Progressive JPEG pass 1. Conversations beyond a single prompt. */}

## Follow-up Prompts

{/* session.sendPrompt(text) to continue within same session.
  Example: start a task, wait for completion, send follow-up. TS + Python CodeGroup. */}

## Resuming Across Sessions

{/* TS only. resume parameter in agent.start().
  - Capture claude_session_id from turn_complete event
  - Pass as resume in new agent.start() call
  - Example: save session ID, create sandbox from checkpoint, resume conversation
  - Note: Python SDK does not support resume. Show HTTP API tab as workaround.
*/}

<Note>
  Resume is currently available in the TypeScript SDK only. Python SDK support is planned.
</Note>

## Interrupting

{/* session.interrupt() to stop the current turn. Both SDKs. */}

## Reconfiguring Mid-Session

{/* session.configure() to change model, tools, etc. while session is running. */}

## Managing Sessions

{/* List, attach, and manage running agent sessions. */}

### Listing Sessions

{/* sandbox.agent.list() — both SDKs. */}

### Attaching to a Running Session

{/* sandbox.agent.attach(sessionId) — reconnect to running session. Both SDKs.
  When to use attach vs resume:
  - attach = same session still running, reconnect to event stream
  - resume = new session continuing old conversation (TS only)
*/}

<Note>
  Agent sessions are SDK-only — there is no CLI command for agents yet.
</Note>
