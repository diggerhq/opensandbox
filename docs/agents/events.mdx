---
title: "Events"
description: "Understanding the agent event stream"
---

{/*
  TODO: Expand this stub into full content.

  GOAL: Explain the common agent events and how to handle them safely.
  The wrapper mostly forwards Claude Agent SDK messages through as-is, so this page should document
  the common events and patterns without pretending the event taxonomy is narrower or more stable than it is.
*/}

## Overview

{/* Brief intro: agent sessions emit typed events via onEvent callback. Each event has a `type` field. */}

## Event Lifecycle

{/* Show a typical successful flow, but note this is illustrative rather than exhaustive:
  ready -> configured -> assistant/system/tool_use_summary/... -> result -> turn_complete
*/}

## Event Reference

{/* Cover the common event families. Each subsection should explain what it means, when it fires,
  the fields people usually care about, and a code example.
  Add a note near the top: preserve unknown fields and tolerate unknown event types.
*/}

### `ready`

{/* Agent process started, waiting for configuration. */}

### `configured`

{/* Agent configured and working on the prompt. */}

### `assistant`

{/* Claude's text output. Do not hard-code a single payload shape; current wrapper comments show `message`,
  and upstream Claude SDK event fields may evolve. Document the common fields and show how to inspect raw events.
*/}

### `tool_use_summary`

{/* Tool summary event from the wrapper/SDK stream. Document common fields only; do not overfit the exact shape. */}

### `system`

{/* System-level messages from the SDK stream. Call out common subtypes like init, task_started, and task_progress. */}

### `tool_progress`

{/* Long-running tool progress updates. Call out common fields like tool_name and elapsed_time_seconds. */}

### `result`

{/* Final result event. Document common fields like subtype plus result or structured_output, but note the payload varies by run/output mode. */}

### `turn_complete`

{/* Turn finished. Fields: claude_session_id (used for resume). Emitted after result. */}

### `interrupted`

{/* Agent was interrupted via session.interrupt(). */}

### `error`

{/* Agent emitted an explicit error event. Message field is common, but preserve unknown fields. */}

## Filtering Events

{/* Practical patterns:
  - Log only assistant messages
  - Capture the final result
  - Build progress indicators from system/task/tool_progress events
  - Preserve unknown events for forward compatibility
*/}

## Error Handling

{/* error events vs onError (stderr) vs rejected promises. When each fires, how to respond. */}
