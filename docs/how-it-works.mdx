---
title: "How It Works"
description: "Architecture and key technical decisions"
---

{/*
  TODO: Expand this stub into full content.

  GOAL: Give technically curious users a clear mental model of the system in under 3 minutes.
  Cover architecture and the key technical decisions users would have opinions about.

  TONE: Concise, technical, opinionated. No marketing. A senior engineer should read this
  and think "these are reasonable choices, I understand the tradeoffs."
*/}

## Overview

{/* One paragraph: your code talks to the control plane, which provisions a Firecracker microVM and returns a worker connectURL/token for data-plane operations. Agents and commands run inside that VM. Events and I/O stream back over WebSocket. */}

## Firecracker microVMs

{/* Why not containers? Each sandbox is a real VM with its own kernel, memory, and disk. Hardware-level isolation (KVM). Boot in ~150ms. Same technology AWS Lambda uses. */}

## Hibernation

{/* VM state is snapshotted so wake is usually fast. Explain warm-restore behavior and the cold-boot fallback path when exact snapshot restore is unavailable. Tradeoff: snapshot size scales with memory allocation. */}

## Checkpoints & Forking

{/* Named snapshots. Forking tries warm restore first and falls back to cold boot if needed. Emphasize the user-visible guarantee: preserved filesystem state and a reproducible starting point, not always literal process continuity. */}

## Templates

{/* Built from Dockerfiles but converted to VM root filesystems (not run as containers). Docker produces a filesystem image, Firecracker boots from it directly. */}

## Networking

{/* Each VM gets a TAP device with NAT. Full outbound internet. Inbound access via preview URLs (reverse-proxied through the host). */}

## Agent Sessions

{/* Claude Agent SDK runs inside the VM as a regular process. SDK spawns Claude, gives it bash + file tools, manages think->act->observe loop. Events parsed from stdout, streamed to your code over WebSocket. */}
