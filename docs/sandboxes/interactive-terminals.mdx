---
title: "Interactive Terminals"
description: "Full PTY sessions inside sandboxes"
---

{/* STUB — Progressive JPEG pass 1. PTY sessions — like SSH but over WebSocket. */}

## What is a PTY Session

{/* A full interactive terminal inside the sandbox. Supports colors, full-screen apps (vim, top). Like SSH but over WebSocket. */}

## Quick Example

{/* Create PTY, send command, read output. TS + Python CodeGroup. */}

<CodeGroup>

```typescript TypeScript
// TODO: sandbox.pty.create({ onOutput }) -> session.send('ls\n') -> close
```

```python Python
# TODO: sandbox.pty.create(on_output=...) -> session.send('ls\n') -> recv() -> close
```

</CodeGroup>

## Creating a PTY

{/* sandbox.pty.create(opts) */}

### PtyOpts

{/* ParamField: cols (default 80), rows (default 24), onOutput callback */}

## PtySession

{/* Methods table:
  - send(data) — both SDKs
  - close() — both SDKs
  - recv() — Python-only (returns bytes; pull-based alternative to onOutput callback)
  - Note: resize() not available in SDKs. HTTP API has POST /sandboxes/:id/pty/:sessionID/resize but no SDK wrapper.
*/}

## Examples

{/* Run interactive commands, pipe stdin. */}

## CLI

{/* oc shell <sandbox-id> — opens interactive terminal. Supports --shell flag (default /bin/bash). */}

<Tip>
  CLI equivalent: [`oc shell`](/cli/shell). Full reference: [TypeScript SDK](/reference/typescript-sdk#pty) · [Python SDK](/reference/python-sdk#pty) · [HTTP API](/reference/api#pty).
</Tip>
