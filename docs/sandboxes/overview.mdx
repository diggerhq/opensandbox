---
title: "Sandboxes"
description: "The compute primitive — full Linux VMs in the cloud"
---

{/* STUB — Progressive JPEG pass 1. The definitive sandbox page: concept + lifecycle + API. */}

## What is a Sandbox

{/* A full Linux virtual machine in the cloud. Isolated environment: own filesystem, network, processes. Think of it as a laptop that sleeps when idle and wakes instantly. */}

## Quick Example

{/* Create -> run command -> kill. TS + Python CodeGroup. */}

<CodeGroup>

```typescript TypeScript
// TODO: Sandbox.create -> exec.run('echo hello') -> kill
```

```python Python
# TODO: Sandbox.create -> exec.run('echo hello') -> kill
```

</CodeGroup>

## Specs

{/* Table:
  - OS: Ubuntu-based Linux
  - Default CPU: 1 vCPU (configurable up to 4 via cpuCount — TS + HTTP; not yet Python)
  - Default memory: 512MB (configurable up to 2048MB via memoryMB — TS + HTTP; not yet Python)
  - Storage: 20GB workspace
  - Network: full outbound internet
  - Pre-installed: Python 3, Node.js, common CLI tools
*/}

## Creating a Sandbox

{/* Sandbox.create(opts) with ParamField reference:
  - Both SDKs: template, timeout, apiKey/api_key, apiUrl/api_url, envs, metadata
  - TS-only: cpuCount, memoryMB
  - Note for Python: use HTTP API for custom resources
*/}

## Connecting to an Existing Sandbox

{/* Sandbox.connect(sandboxId) — TS + Python CodeGroup */}

## Lifecycle

{/* Status states: running, hibernated, stopped, error.
  Text-based lifecycle: running <-> hibernated -> stopped; running -> error
  Rolling timeout: resets on every operation, default 300s.
  What happens on timeout: auto-hibernate if possible, else stop.
*/}

## Hibernation & Wake

{/* Like closing a laptop lid: memory + disk snapshotted, sandbox ID stays same.
  - sandbox.hibernate(), sandbox.wake() — TS SDK
  - Resume in seconds, no cost while hibernated
  - Auto-triggered on idle timeout
  - Difference from checkpoints: hibernation = transparent resume; checkpoints = named snapshots you fork from
  - Note: Python SDK doesn't have hibernate/wake. HTTP API: POST /api/sandboxes/:id/hibernate, wake. CLI: oc sandbox hibernate, wake.
*/}

<Note>
  `hibernate()` and `wake()` are available in the TypeScript SDK and CLI. Python SDK support is planned — use the HTTP API directly for now.
</Note>

## Other Methods

{/* kill(), isRunning()/is_running(), setTimeout()/set_timeout() */}

## Properties

{/* sandboxId, agent, exec, files, pty
  Python-specific: async with Sandbox.create() as sandbox: (context manager, auto-kills on exit)
*/}

## Related

{/* Links to: Agents overview (agents run inside sandboxes), Running Commands, Files, Terminals, Checkpoints, Templates */}
