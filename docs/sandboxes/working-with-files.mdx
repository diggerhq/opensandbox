---
title: "Working with Files"
description: "Read, write, and manage files inside a sandbox"
---

{/*
  TODO: Expand this stub into full content.

  GOAL: Complete guide to reading, writing, and managing files inside sandboxes.

  ABSORBS: sdks/typescript/filesystem.mdx, sdks/python/filesystem.mdx, working-with-files.mdx (root).

  KEY NOTES:
  - Current structure is already good — merge TS/Python into CodeGroups and ensure consistency.
  - exists() is a client-side SDK wrapper built on a normal file read request, not a separate HTTP endpoint.
*/}

## Reading Files

{/* sandbox.files.read(path) — returns string content. TS + Python CodeGroup. */}

### Read as Bytes

{/* sandbox.files.readBytes(path) / read_bytes(path) — returns binary. TS + Python CodeGroup. */}

## Writing Files

{/* sandbox.files.write(path, content) — text and binary. TS + Python CodeGroup. */}

## Listing Directories

{/* sandbox.files.list(path) — returns EntryInfo[]. TS + Python CodeGroup. */}

### EntryInfo

{/* Table: name, isDir/is_dir, path, size.
  Do not document modTime/mod_time on EntryInfo unless the SDK starts returning it.
*/}

## Managing Files

{/* make_dir, remove, exists. TS + Python CodeGroup for each. */}

### Create Directory

{/* sandbox.files.makeDir(path) / make_dir(path) */}

### Remove

{/* sandbox.files.remove(path) */}

### Check Existence

{/* sandbox.files.exists(path) — client-side convenience wrapper, not a dedicated HTTP endpoint. Both SDKs. */}

## Examples

{/* Upload & run a script. Copy files between paths. */}

<Tip>
  Full reference: [TypeScript SDK](/reference/typescript-sdk#filesystem) · [Python SDK](/reference/python-sdk#filesystem) · [HTTP API](/reference/api#filesystem).
</Tip>
