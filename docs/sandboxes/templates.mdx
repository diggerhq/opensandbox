---
title: "Templates"
description: "Pre-built base images for sandboxes"
---

{/*
  TODO: Expand this stub into full content.

  GOAL: Complete guide to templates — pre-built base images for sandboxes.
  Entity page: concept + API + examples.

  ABSORBS: sdks/typescript/templates.mdx, sdks/python/templates.mdx.

  KEY NOTES:
  - Templates is not a Sandbox property
  - TS: Templates (plural), Python: Template (singular)
  - Do not invent static helper APIs. Document the actual exported construction/usage pattern for each SDK.
  - Python template ergonomics are currently awkward; if no public construction helper exists, either improve the SDK first or make this section HTTP-first.
  - Template status depends on template type. Dockerfile builds currently return ready immediately; sandbox-snapshot templates use processing -> ready.
  - Be explicit about `default` vs legacy `base` naming where relevant
  - Default template: derive pre-installed list from Dockerfile.default
*/}

## What is a Template

{/* Pre-built base image that sandboxes start from. The `default` template includes Ubuntu, Python, Node.js. Build custom templates from Dockerfiles to skip setup time. */}

## Quick Example

{/* Build template -> create sandbox from it.
  Use real SDK construction only. If Python still lacks a public ergonomic entry point, use HTTP for the primary example and call the SDK gap out explicitly.
*/}

<CodeGroup>

```typescript TypeScript
// TODO: use the real exported Templates construction pattern, then build/list/get/delete from that instance
```

```python Python
# TODO: only add a Python SDK example if there is a public construction path worth documenting; otherwise use HTTP and note the SDK ergonomics gap
```

</CodeGroup>

## Default Template

{/* What's pre-installed: Ubuntu, Python 3, Node.js, common CLI tools. Derived from Dockerfile.default. */}

## API Reference

<Note>
  Templates is a standalone class, not a property on Sandbox. TypeScript exports `Templates` (plural), Python exports `Template` (singular).
</Note>

### Build Template

{/* Document the real SDK invocation pattern and the HTTP API.
  Do not publish fictional static helpers if the SDK requires an instance.
*/}

### List Templates

{/* Document the real SDK callable surface for listing templates plus the HTTP API.
  Avoid implying Python classmethods if the SDK does not expose them.
*/}

### Get Template

{/* Document the real SDK callable surface for fetching a template plus the HTTP API. */}

### Delete Template

{/* Document the real SDK callable surface for deleting a template plus the HTTP API. */}

### Using with Sandbox.create

{/* Sandbox.create({ template: 'my-template' }) */}

## TemplateInfo

{/* Fields: verify against the current handler/SDK mapping before writing the final page.
  Status is not one universal enum across all template creation flows.
*/}

## Example

{/* Template with specific language/framework pre-installed (e.g., Node 20 + pnpm). */}

<Tip>
  Full reference: [TypeScript SDK](/reference/typescript-sdk#templates) · [Python SDK](/reference/python-sdk#template) · [HTTP API](/reference/api#templates).
</Tip>
